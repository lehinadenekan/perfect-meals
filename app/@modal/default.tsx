// app/@modal/default.tsx

// This component renders null 
// It serves as the default fallback for the @modal slot when no specific 
// intercepting route matches the current URL.

export default function Default() {
  return null;
} 